# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
target/
.cache/

# Cairo / Scarb
.snfoundry_cache/
*.sierra.json
*.casm.json

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key

# Editor
.vscode/*
!.vscode/extensions.json
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vendored third-party repos — cloned fresh per docs/decisions, not committed
vendor/

# strk20.json is tracked deliberately — do NOT ignore it
docs/HANDOVER.md
